name: Deploy AWS

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: Container image tag
        required: false
        default: latest

concurrency:
  group: deploy-aws-production
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production-aws
    env:
      AWS_REGION: ${{ vars.AWS_REGION }}
      TF_VAR_aws_region: ${{ vars.AWS_REGION }}
      TF_VAR_environment: production
      TF_VAR_image_tag: ${{ inputs.image_tag || github.sha }}
      TF_VAR_mongo_uri: ${{ secrets.MONGO_URI }}
      TF_VAR_openai_api_key: ${{ secrets.OPENAI_API_KEY }}
      TF_VAR_kafka_broker: ${{ secrets.KAFKA_BROKER }}
      TF_VAR_kafka_username: ${{ secrets.KAFKA_USERNAME }}
      TF_VAR_kafka_password: ${{ secrets.KAFKA_PASSWORD }}
      TF_VAR_firebase_service_account_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Initialize Terraform
        working-directory: infra/aws
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=comconnect/production.tfstate" \
            -backend-config="region=${{ vars.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}"

      - name: Bootstrap container registries
        working-directory: infra/aws
        run: terraform apply -auto-approve -target=aws_ecr_repository.repository

      - uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push service images
        env:
          IMAGE_TAG: ${{ inputs.image_tag || github.sha }}
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
        run: |
          REGISTRY="$AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com"
          docker build -f gateway/Dockerfile -t "$REGISTRY/comconnect-production-gateway:$IMAGE_TAG" .
          docker build -f backend/Dockerfile -t "$REGISTRY/comconnect-production-backend:$IMAGE_TAG" .
          docker build -f ai-service/Dockerfile -t "$REGISTRY/comconnect-production-ai-engine:$IMAGE_TAG" .
          docker push "$REGISTRY/comconnect-production-gateway:$IMAGE_TAG"
          docker push "$REGISTRY/comconnect-production-backend:$IMAGE_TAG"
          docker push "$REGISTRY/comconnect-production-ai-engine:$IMAGE_TAG"

      - name: Apply AWS infrastructure
        working-directory: infra/aws
        run: terraform apply -auto-approve

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        working-directory: frontend
        env:
          CI: "true"
        run: |
          npm ci --legacy-peer-deps
          npm run build

      - name: Publish frontend and invalidate CloudFront
        working-directory: infra/aws
        run: |
          BUCKET="$(terraform output -raw frontend_bucket)"
          DISTRIBUTION="$(terraform output -raw cloudfront_distribution_id)"
          aws s3 sync ../../frontend/build "s3://$BUCKET" --delete
          aws cloudfront create-invalidation --distribution-id "$DISTRIBUTION" --paths "/*"
